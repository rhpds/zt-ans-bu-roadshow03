---

antora:
  name: modules
  dir: www
  modules:
    - name: module-01
      label: "The day the application died"
      solveButton: true
      scripts:
        - solve
        - validation
    ## module-02 disabled — Insights remediation workflow replaced by Lightspeed which requires RHC agent + elevated RBAC roles
    # - name: module-02
    #   label: "All your vulnerabilities are belong to us"
    #   solveButton: false
    - name: module-03
      label: "Oh no whats next, the network ?"
      solveButton: true
      scripts:
        - solve
        - validation

tabs:
  - name: aap
    url: https://control-${guid}.${domain}/
    external: false
  - name: ">_ S.E.A.N"
    url: /wetty_sean
  - name: Webapp
    url: https://podman-host-${guid}.${domain}/index.html
    external: true
  - name: ServiceNow
    url: https://ansible.service-now.com
    external: true
